{
  "name": "tsdiapi-meta",
  "description": "A TSDIAPI plugin for automatic OpenAPI metadata generation, schema validation, and extended route information extraction.",
  "generators": [
    {
      "name": "controller",
      "description": "Generate a new controller in the API",
      "files": [
        {
          "source": "generators/controller/*.*",
          "destination": ".",
          "overwrite": false,
          "isHandlebarsTemplate": true
        }
      ],
      "postMessages": [
        "✅ Controller {{name}} created successfully!"
      ]
    }
  ],
  "variables": [
    {
      "name": "META_AUTO_REGISTER_CONTROLLERS",
      "type": "boolean",
      "default": true,
      "configurable": true,
      "description": "Automatically register controllers in the meta system",
      "inquirer": {
        "type": "confirm",
        "message": "Do you want to enable automatic controller registration?",
        "default": true
      }
    }
  ],
  "postMessages": [
    "✅ Plugin Meta has been successfully installed!"
  ]
}